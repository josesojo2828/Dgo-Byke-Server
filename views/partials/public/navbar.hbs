
<nav class="bg-white shadow-sm fixed w-full z-10">
    <div class="container mx-auto px-6 py-3">
        <div class="flex justify-between items-center">
            <div class="flex items-center">
                <div class="w-10 h-10 gradient-bg rounded-full flex items-center justify-center mr-3">
                    <i class="fas fa-university text-white"></i>
                </div>
                <h1 class="text-xl font-bold gradient-text">{{  __ 'common.app_name' lang }}</h1>
            </div>

            <div class="hidden md:flex items-center space-x-8">
                <a href="#home" class="text-gray-700 hover:text-blue-600 transition font-medium">{{  __ 'common.nav.home' lang }}</a>
                <a href="#services" class="text-gray-700 hover:text-blue-600 transition font-medium">{{  __ 'common.nav.services' lang }}</a>
                <a href="#about" class="text-gray-700 hover:text-blue-600 transition font-medium">{{  __ 'common.nav.about' lang }}</a>
                <a href="#testimonials" class="text-gray-700 hover:text-blue-600 transition font-medium">{{  __ 'common.nav.testimonials' lang }}</a>

                <div class="flex items-center space-x-3 border-l border-gray-200 pl-6 ml-2">
                    {{#ifeq lang 'es'}}
                        <span class="text-blue-600 font-bold text-sm">Español</span>
                    {{else}}
                        <a href="?lang=es" class="lang-switcher text-gray-700 hover:text-blue-600 transition font-medium text-sm" data-lang="es">Español</a>
                    {{/ifeq}}

                    <span class="text-gray-300">|</span>

                    {{#ifeq lang 'en'}}
                        <span class="text-blue-600 font-bold text-sm">English</span>
                    {{else}}
                        <a href="?lang=en" class="lang-switcher text-gray-700 hover:text-blue-600 transition font-medium text-sm" data-lang="en">English</a>
                    {{/ifeq}}
                </div>

                <a href="/login" class="px-4 py-2 gradient-bg text-white rounded-lg font-medium hover:opacity-90 transition ml-4">
                    {{ __ 'common.auth.login' lang }}
                </a>
            </div>

            <div class="md:hidden">
                <button id="mobile-menu-button" class="text-gray-600 hover:text-blue-600">
                    <i class="fas fa-bars text-2xl"></i>
                </button>
            </div>
        </div>
    </div>

    <div id="mobile-menu" class="hidden md:hidden bg-white border-t shadow-lg">
        <div class="container mx-auto px-6 py-4 flex flex-col space-y-4">
            <a href="#home" class="text-gray-700 hover:text-blue-600 transition font-medium py-2">Home</a>
            <a href="#services" class="text-gray-700 hover:text-blue-600 transition font-medium py-2">Services</a>
            <a href="#about" class="text-gray-700 hover:text-blue-600 transition font-medium py-2">About</a>
            <div class="border-t border-gray-100 pt-4 mt-4 flex justify-center space-x-4">
                {{#ifeq lang 'es'}}
                    <span class="text-blue-600 font-bold">Español</span>
                {{else}}
                    <a href="#" class="lang-switcher text-gray-700 hover:text-blue-600" data-lang="es">Español</a>
                {{/ifeq}}
            
                <span class="text-gray-400">|</span>
            
                {{#ifeq lang 'en'}}
                    <span class="text-blue-600 font-bold">English</span>
                {{else}}
                    <a href="#" class="lang-switcher text-gray-700 hover:text-blue-600" data-lang="en">English</a>
                {{/ifeq}}
            </div>
            <a href="#contact"
                class="px-4 py-2 gradient-bg text-white rounded-lg text-center font-medium hover:opacity-90 transition mt-4">
                Schedule
                Meeting
            </a>
        </div>
    </div>
</nav>
